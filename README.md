# Automated-Supply-Chain-Reporting-Pipeline
A Python script that consolidates multiple raw Excel/CSV planner exports — each from a different regional team, each with its own formatting quirks — into a single cleaned dataset, then auto-generates a summary pivot report. Built to remove the manual weekly data-wrangling an analyst would otherwise do by hand. Tools: Python, pandas, openpyxl
